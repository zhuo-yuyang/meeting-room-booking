server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhuoyuyang_qimou?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: system
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
      timeout: 5000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto

jwt:
  # Base64 编码的密钥（注意：YAML 中不需要引号，但如果包含特殊字符需要用引号）
  secret: wF2/0dKAl+O8RuaiHg9rw52yPSiijiEG1I6cp0XmVFY=
  expiration: 86400000

logging:
  level:
    com.booking: DEBUG
